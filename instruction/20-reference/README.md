# Reference

Appendix material — quick lookup, not a learning path. Cross-referenced
from other topic files rather than read start-to-end.

## Planned topics

- `glossary.md` — terms used throughout the handbook (upstream, backpressure, head-of-line blocking, etc.) with a one-line definition and a pointer to the topic file that covers each in depth
- `cheatsheet.md` — quick-reference tables (HTTP status codes worth handling specially, common `errno`s a proxy will see, TLS handshake steps) for lookup while implementing
